import styled from 'styled-components';

import { containerBase } from './styles';

// Configure layout Body container
export const Body = styled.section`
  ${containerBase};
  min-height: 100vh;
  flex-direction: column;
`;
